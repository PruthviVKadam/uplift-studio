# Pinned for reproducibility on Python 3.14.
# scikit-uplift 0.5.1 uses sklearn.utils.extmath.stable_cumsum, removed in sklearn 1.10 —
# so scikit-learn is pinned < 1.10 deliberately.
scikit-uplift==0.5.1
scikit-learn==1.9.0
pandas==3.0.3
numpy==2.5.0
scipy==1.18.0
plotly>=5.22
streamlit>=1.36
pytest>=8.0
